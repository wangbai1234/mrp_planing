# 最终交付审计 Prompt

请复制下方正文，在 09 阶段完成后使用：

```text
对 MRP Phase 1 做最终只读优先审计。读取 project.md、所有 Harness 契约、workflow.yaml、00-09 阶段报告、traceability-matrix、需求、规则库和 UAT。

验证：
1. 00-09 全部 DONE 且依赖顺序正确；
2. R001-R017 每条都有代码、API/UI 和测试证据；
3. 技术栈没有未审批的中间件；
4. 相同输入 checksum 的计算结果一致；
5. 没有日能力换算、滚动一周按钮、独立系统设置页和 Phase 2 越界；
6. 导入跨年、料号文本、staging 和确认幂等通过；
7. 工厂数据域、JWT、审计和上传安全通过；
8. Docker 可从空库启动，迁移可重复，Worker 中断可恢复；
9. UAT 全部执行，阻断问题为零；
10. 运行、备份、恢复和已知限制文档完整。

输出最终 PASS/FAIL 和证据索引。FAIL 时只给阻断问题和建议回到的阶段，不得虚构完成。PASS 后生成最终交付报告，但不要执行生产部署。
```
