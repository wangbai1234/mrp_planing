# MRP Agent Entry

任何大模型或编码代理进入本仓库后，必须按以下顺序读取：

1. 根目录 `project.md`
2. `.harness/execution-laws.md`（执行铁律，不可跳过）
3. `.harness/README.md`
4. 当前任务对应的 `.harness/prompts/*.md`
5. 对应的原型文件（前端任务必须读 `prototype/` 目录）
6. **前端开发必须读 `assets/import-design-rules.md`**
7. **涉及 Excel 导入功能必须读 `.harness/import-rules.md`**
8. **涉及列表页面必须读 `.harness/list-rules.md`**

不得跳过阶段门禁，不得引入 `project.md` 未允许的中间件，不得把 Phase 2 功能放入 Phase 1。任务完成报告遵循 `.harness/task-state.md`。

## Bug修复流程（强制执行）

遇到用户报告bug时，**必须按以下流程执行**：

1. **理解问题** - 仔细分析错误信息，定位根本原因
2. **制定修复方案** - 列出具体修改内容
3. **执行修复** - 按方案修改代码
4. **自我复核** - 验证修复是否正确，检查是否有遗漏
5. **确认无误后** - 才能告诉用户结果

**禁止：**
- ❌ 修改后直接交给用户，不进行复核
- ❌ 只看表面错误，不分析根本原因
- ❌ 多次尝试失败后不总结规律

**复核清单：**
- 修改的文件是否正确保存
- 前端是否重新编译
- 后端是否重启
- API是否能正常响应
- 浏览器是否需要清除缓存

## 绝对禁止

- ❌ 只验证编译和单元测试就报告"完成"——必须实际启动服务并用浏览器/curl 验证
- ❌ 不读原型就开始写前端——原型是 UI 契约，不是参考
- ❌ 修改已应用的 Flyway 迁移——必须创建新迁移文件
- ❌ 在 UI 中使用 emoji
- ❌ 擅自修改技术栈配置（Java版本、Node版本、框架版本等）——这是血泪教训，遇到环境问题必须先询问用户

## 环境问题处理（强制执行）

当遇到编译/运行错误时，**必须按以下流程执行**：

1. 分析错误原因（读错误信息）
2. 判断是否为环境问题（JDK版本、Node版本、依赖版本等）
3. **如果是环境问题 → 立即停止，只输出以下格式的消息，然后等待用户回复：**

```
【环境问题】当前环境不满足项目要求。
- 问题：[具体描述，如：Java 17，项目要求 Java 21]
- 请先手动配置正确的环境，然后告诉我继续。

我不会帮你安装、切换或修改任何版本配置。
```

**禁止：**
- ❌ 主动执行 `brew install`、`sdk install`、`apt install` 等安装命令
- ❌ 主动修改 `pom.xml`、`package.json`、`.nvmrc` 等版本配置
- ❌ 提供"让我帮你安装"的选项
- ❌ 询问"是否需要我帮你安装"——直接拒绝

**这不是建议，是硬性规则。违反此规则等于破坏项目契约。**

---

## 三层验证（强制执行）

每次代码改动后，**必须按顺序执行以下三层验证**：

| 层 | 命令 | 何时执行 |
|---|---|---|
| 编译 | `./mvnw compile` 或 `npm run build` | 每次改动后 |
| 单元测试 | `./mvnw test` | 每次改动后 |
| 集成联调 | 启动服务 + 浏览器/curl 验证 | 每个任务完成后 |

**执行指令：**
1. 改完代码 → 立即运行编译
2. 编译通过 → 立即运行测试
3. 测试通过 → 启动服务，用浏览器访问页面或 curl 调用 API
4. 验证数据正确返回 → 才能报告"完成"

**禁止：**
- ❌ 只运行 `./mvnw test` 就报告"完成"
- ❌ 只运行 `npm run build` 就报告"完成"
- ❌ 不实际启动服务就声称"功能正常"

---

## 前端开发规则（强制执行）

### 必须读原型

开始前端任务前，**必须先读原型文件**：

```
1. 读 prototype/ 目录下对应页面的原型
2. 对照原型确认：布局、颜色、组件结构、表格列、弹窗
3. 然后才能开始写代码
```

**禁止：**
- ❌ 不读原型就开始写前端
- ❌ 把原型的 vanilla JS 直接翻译成 Vue 语法
- ❌ 用硬编码数据代替 API 调用

### 必须用 Element Plus

所有 UI 组件**必须使用 Element Plus**：

```typescript
// 正确
import { ElButton, ElTable } from 'element-plus'

// 错误 - 自己写 CSS 代替组件库
<div class="my-button">点击</div>
```

**禁止：**
- ❌ 不用 Element Plus 自己写 CSS 组件
- ❌ 用 emoji 代替图标或状态标识

### 必须处理空数据和错误

每个页面**必须处理以下场景**：

```typescript
// 1. 空数据
<div v-if="data.length === 0">
  <el-empty description="暂无数据" />
</div>

// 2. 加载状态
<div v-loading="loading">...</div>

// 3. 错误处理
try {
  const res = await get('/api/...')
} catch (e: any) {
  ElMessage.error('加载失败: ' + e.message)
}
```

**禁止：**
- ❌ 页面白屏不处理
- ❌ 空数据时显示空白表格
- ❌ 错误时无任何提示

---

## 数据库迁移规则（强制执行）

### 迁移文件不可变

Flyway 迁移文件一旦应用，**禁止修改其内容**：

```
V001__init_schema.sql  ← 已应用，禁止修改
V002__add_table.sql    ← 已应用，禁止修改
V003__new_change.sql   ← 新增，可以修改（未应用前）
```

**正确做法：需要变更时创建新迁移文件**
```bash
# 如果需要修改 V001 的内容，必须创建 V003
V003__alter_table_xxx.sql
```

**禁止：**
- ❌ 修改已应用的 V001、V002 等迁移文件
- ❌ 删除已应用的迁移文件
- ❌ 重新编号迁移文件

---

## 技术栈配置规则（强制执行）

以下配置属于项目契约，**任何情况下禁止修改**：

| 配置项 | 文件 | 当前值 |
|--------|------|--------|
| Java 版本 | `pom.xml` | 21 |
| Node 版本 | `package.json` | LTS |
| Spring Boot 版本 | `pom.xml` | 3.4.1 |
| 框架选型 | `pom.xml` / `package.json` | 见依赖列表 |

**遇到环境问题时的正确响应：**

```
【环境问题】当前环境不满足项目要求。
- 问题：[具体描述]
- 请先手动配置正确的环境，然后告诉我继续。
```

**禁止：**
- ❌ 修改 `pom.xml` 中的 `<java.version>`
- ❌ 修改 `package.json` 中的 `engines.node`
- ❌ 降级依赖版本来适配本地环境
- ❌ 以"本地环境不支持"为由擅自修改项目配置

---

## 原型驱动规则（强制执行）

原型是**UI 布局和样式参考**，不是代码模板：

| 原型内容 | 如何使用 |
|----------|----------|
| 布局结构 | 参考布局，用 Vue 3 + Element Plus 实现 |
| 颜色/字体 | 参考 CSS 变量，但组件用 Element Plus |
| 交互逻辑 | 用真实 API 调用替换硬编码数据 |
| vanilla JS | **禁止直接翻译**，用 Vue 语法重写 |

**正确流程：**
```
1. 读原型 → 理解布局和交互意图
2. 用 Element Plus 组件实现相同布局
3. 用真实 API 替换硬编码数据
4. 用 Vue 响应式替换 DOM 操作
```

**禁止：**
- ❌ 把原型代码直接复制粘贴
- ❌ 把原型的 vanilla JS 直接翻译成 Vue 语法
- ❌ 用原型的 CSS 代替 Element Plus 组件

---

## 前端开发速查表（每次开发前必读）

### 页面布局
```vue
<div class="page-header">
  <h1 class="page-title">标题</h1>
  <div class="page-actions">按钮</div>
</div>
<div class="filter-bar">筛选</div>
<div class="table-card">表格+分页</div>
```
**不要添加 page-desc 描述文字**

### 按钮规范（强制）
| 功能 | 文案 | 样式 |
|------|------|------|
| 导入 | 导入 | `type="primary"` 蓝色 |
| 导出 | 导出 | `type="primary"` 蓝色 |
| 新增 | 新增 | `type="primary"` 蓝色 |
| 删除 | 删除 | `type="danger" link` 红链接 |
| 取消 | 取消 | 默认白色 |
| 确认 | 确认 | `type="primary"` 蓝色 |
| 下载模板 | 下载模板 | `type="primary"` 蓝色 |

**禁止：使用plain修饰符、多种颜色按钮、多余文字**

### 筛选框尺寸
- 搜索框：`width: 220px`
- 下拉筛选：`width: 140px`

### 弹窗风格（飞书风格）
- 圆角：12px
- 标题：20px #1F2329
- 主色：#1677FF
- 底部按钮：取消 + 确认/开始导入

### 分页
- 默认每页20条
- 中文化：`layout="total, sizes, prev, pager, next"`

### 颜色规范
- 主色：#1677FF
- 标题：#1F2329
- 正文：#4E5969
- 辅助：#86909C
- 错误：#F53F3F
- 成功：#00B42A

---

## Excel 导入功能开发规则（强制执行）

### 必读文档

开发任何导入功能前，**必须先读取**：
```
.harness/import-rules.md
```

### 核心要点

1. **模板生成**：表头行 + 提示行 + 示例数据，月份列动态生成
2. **解析器**：列索引必须与模板一致，数据起始行跳过表头和提示行
3. **原始数据保存**：必须保存 rawRows 和 headers，用于生成错误报告
4. **稀疏Excel处理**：空列必须填充到固定列数
5. **数量类型**：使用 BigDecimal，禁止 float/double
6. **错误报告**：错误原因在最后一列，不在中间列
7. **前端展示**：有错误时显示红色提示 + 下载按钮 + 禁用导入

### 检查清单

开发导入功能后，必须确认：
- [ ] 模板列顺序与解析器列索引一致
- [ ] 保存了 rawRows 和 headers
- [ ] 稀疏Excel已填充空列
- [ ] 数量使用 BigDecimal
- [ ] 错误原因在最后一列
- [ ] 前端有错误时禁用导入按钮
