<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mrp.inventory.repository.InventoryItemMapper">

    <insert id="insertItems">
        INSERT INTO inventory_item (snapshot_id, product_line, inventory_category, material_id,
            project_model, material_name, supplier_name, product_mode,
            odm_supplier_qty, xa400_qty, xa378_qty, xa226_qty, shipping_available_qty,
            classification, barcode, remark, order_pending_qty, sales_status, parent_record)
        VALUES
        <foreach collection="list" item="d" separator=",">
            (#{d.snapshotId}, #{d.productLine}, #{d.inventoryCategory}, #{d.materialId},
             #{d.projectModel}, #{d.materialName}, #{d.supplierName}, #{d.productMode},
             #{d.odmSupplierQty}, #{d.xa400Qty}, #{d.xa378Qty}, #{d.xa226Qty}, #{d.shippingAvailableQty},
             #{d.classification}, #{d.barcode}, #{d.remark}, #{d.orderPendingQty}, #{d.salesStatus}, #{d.parentRecord})
        </foreach>
    </insert>

    <select id="selectBySnapshotId" resultType="com.mrp.inventory.domain.InventoryItem">
        SELECT * FROM inventory_item WHERE snapshot_id = #{snapshotId} ORDER BY id
    </select>

    <select id="selectPageBySnapshotId" resultType="com.mrp.inventory.domain.InventoryItem">
        SELECT * FROM inventory_item WHERE snapshot_id = #{snapshotId} ORDER BY id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countBySnapshotId" resultType="int">
        SELECT COUNT(*) FROM inventory_item WHERE snapshot_id = #{snapshotId}
    </select>

    <delete id="deleteBySnapshotId">
        DELETE FROM inventory_item WHERE snapshot_id = #{snapshotId}
    </delete>
</mapper>
