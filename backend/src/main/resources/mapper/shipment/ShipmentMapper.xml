<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mrp.shipment.repository.ShipmentMapper">

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO shipment_batch (source, status, created_by)
        VALUES (#{source}, #{status}, #{createdBy})
    </insert>

    <select id="selectBatchById" resultType="com.mrp.shipment.domain.ShipmentBatch">
        SELECT * FROM shipment_batch WHERE id = #{id}
    </select>

    <select id="selectLatestBatch" resultType="com.mrp.shipment.domain.ShipmentBatch">
        SELECT * FROM shipment_batch ORDER BY id DESC LIMIT 1
    </select>

    <insert id="insertDetails">
        INSERT INTO shipment_detail (batch_id, material_id, plan_month, shipped_qty)
        VALUES
        <foreach collection="list" item="d" separator=",">
            (#{d.batchId}, #{d.materialId}, #{d.planMonth}, #{d.shippedQty})
        </foreach>
    </insert>

    <select id="selectDetailsByBatchId" resultType="com.mrp.shipment.domain.ShipmentDetail">
        SELECT * FROM shipment_detail WHERE batch_id = #{batchId} ORDER BY material_id, plan_month
    </select>

    <select id="countDetailsByBatchId" resultType="int">
        SELECT COUNT(*) FROM shipment_detail WHERE batch_id = #{batchId}
    </select>

    <delete id="deleteDetailsByBatchId">
        DELETE FROM shipment_detail WHERE batch_id = #{batchId}
    </delete>
</mapper>
