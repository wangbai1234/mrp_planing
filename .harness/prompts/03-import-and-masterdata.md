# Prompt 03：实现 Excel 导入和主数据

前置：数据库迁移通过。

读取 `import-contract.md`、模板、真实样本和 `assets/MRP变更影响矩阵.md`。实现经营计划、库存快照、CRM 出货、产能配置的导入/维护：

- 上传文件保存到 Docker 挂载卷并记录 checksum；
- Apache POI SAX/Event 流式解析；
- 先 staging，整表校验、错误报告、预览，用户确认后正式入库；
- 经营计划读取真实 Excel 日期年月，正确识别 2026-12 -> 2027-01，不按文件名猜年份；
- 料号按文本读取，拦截日期、科学计数法、空值和截断；
- 校验 6 个月连续、无重复；
- 库存按工厂分开，不良品仓不计入；
- 产能按周维护，整行新增/编辑，周产能>0；
- 幂等键和数据库唯一约束防止重复确认。

不要在本任务实现排产计算。用真实样本和模板写 JUnit 5 测试，并记录行列级错误。输出 API 草案、样本测试报告和任务报告。完成后状态设为 REVIEW，等待复核。
